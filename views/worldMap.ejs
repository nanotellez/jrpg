<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>Multi-player JRPG</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" />
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>
    <link rel="stylesheet" type="text/css" href="css/world-style.css" />
    <script src="js/map-movement.js"></script>
    <script src="js/chat.js"></script>
</head>

<body>
    <audio id='pageaudio' autoplay loop>
        <source src="./media/overworld.mp3" type="audio/mpeg">
        Your browser does not support the audio element.
    </audio>
    <br>
    <br>
    <div class="container">
        <div class="row justify content around">
            <div id="current-map-tile" class="d-flex col-7 box rounded border border-light justify-content-center">
                <div class="row">
                    <div class="spacer"></div>
                    <div id="current-map">

                    </div>
                </div>
            </div>
            <div class="side-items col-5">
                <div id="world-map-tile" class="d-flex box rounded border border-light justify-content-center">
                    <div class="row">
                        <div class="spacer"></div>
                        <div class="map">
                            <% for (var y = 0; y < gridSize; y++) { %>
                            <div class="row">
                                <% for (var x = 0; x < gridSize; x++) { %>
                                <div class="gridbox-world div-<%=x%>-<%=y%>"></div>
                                <% } %>
                            </div>
                            <% } %>
                        </div>
                    </div>
                </div>
                <div id="world-chat" class="box rounded border border-light">
                    <div id="chathistory" class="" playername="<%= player.name %>">

                    </div>
                    <div class="row col-11 justify-content-center">
                        <input type="text" id="messagetext" class="form-control col-10"/>
                        <button id="sendbtn" class="btn btn-secondary btn-block col-2">Send</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>